# Auth session (contains live tokens — never commit)
session.json

# Supabase credentials if you ever move them to a config file
config.json
*.env
.env*

# nlohmann/json if you drop the header into the repo
include/nlohmann/

# SFML and other libs if vendored locally
libs/
external/
vendor/

# Build folder
build/

# Test binaries
tests/test_*
!tests/test_*.cpp

# GCC/Clang generated
*.d
*.a
*.so
*.dll
*.exe

# Valgrind / sanitizer output
vgcore.*
*.log

# Packaging
*.tar.gz
*.zip